import{NextRequest,NextResponse}from"next/server";export function proxy(request:NextRequest){if(request.nextUrl.pathname.startsWith("/api/")&&!['GET','HEAD','OPTIONS'].includes(request.method)){const site=request.headers.get("sec-fetch-site"),origin=request.headers.get("origin");if(site==="cross-site"||(origin&&new URL(origin).origin!==request.nextUrl.origin))return NextResponse.json({error:"Cross-site request blocked"},{status:403})}return NextResponse.next()}export const config={matcher:["/api/:path*"]};
