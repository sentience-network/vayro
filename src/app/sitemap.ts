import type{MetadataRoute}from"next";import{db}from"@/lib/db";export const dynamic="force-dynamic";export default async function sitemap():Promise<MetadataRoute.Sitemap>{const base=process.env.NEXT_PUBLIC_APP_URL||"https://vayro.onrender.com",listings=await db.listing.findMany({where:{status:"ACTIVE",NOT:{details:{path:["_photoSource"],equals:"VAYRO_REPRESENTATIVE"}}},select:{slug:true,updatedAt:true}});return[{url:base,lastModified:new Date(),priority:1},{url:`${base}/browse`,lastModified:new Date(),priority:.9},{url:`${base}/pricing`,lastModified:new Date(),priority:.8},{url:`${base}/perks`,lastModified:new Date(),priority:.8},{url:`${base}/partners`,lastModified:new Date(),priority:.7},{url:`${base}/contact`,lastModified:new Date(),priority:.6},{url:`${base}/safety`,lastModified:new Date(),priority:.7},...listings.map(x=>({url:`${base}/listings/${x.slug}`,lastModified:x.updatedAt,priority:.8}))]}
