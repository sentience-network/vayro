import{NextRequest,NextResponse}from"next/server";import{z}from"zod";import{db}from"@/lib/db";import{requireUser}from"@/lib/auth";const schema=z.object({subject:z.string().trim().min(3).max(120),message:z.string().trim().min(10).max(4000)});export async function POST(r:NextRequest){try{const u=await requireUser(),p=schema.safeParse(await r.json());if(!p.success)return NextResponse.json({error:p.error.issues[0].message},{status:400});return NextResponse.json(await db.supportTicket.create({data:{userId:u.id,...p.data}}))}catch{return NextResponse.json({error:"Login required"},{status:401})}}
