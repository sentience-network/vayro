import { NextRequest,NextResponse } from "next/server";import {db}from"@/lib/db";import{requireUser}from"@/lib/auth";import{listingSchema}from"@/lib/validation";import{resolveListingPhotos}from"@/lib/listing-images";
export async function POST(request:NextRequest){try{const user=await requireUser();const parsed=listingSchema.safeParse(await request.json());if(!parsed.success)return NextResponse.json({error:parsed.error.issues[0].message},{status:400});const{photoUrls:submitted,...rest}=parsed.data;const{photoUrls,details}=resolveListingPhotos(rest.category,submitted,rest.details);const slug=`${rest.title.toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/(^-|-$)/g,"")}-${Date.now().toString(36)}`;const listing=await db.listing.create({data:{...rest,details,slug,ownerId:user.id,photos:{create:photoUrls.map((url,position)=>({url,position,alt:details._photoSource==="VAYRO_REPRESENTATIVE"?`Representative ${rest.category} image supplied by Vayro`:rest.title}))}},select:{slug:true}});await db.user.update({where:{id:user.id},data:{isOwner:true}});return NextResponse.json(listing)}catch{return NextResponse.json({error:"Unauthorized or invalid request"},{status:401})}}
