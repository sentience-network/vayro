import{NextResponse}from"next/server";import{db}from"@/lib/db";import{requireUser}from"@/lib/auth";export async function POST(_:Request,{params}:{params:Promise<{id:string}>}){try{const u=await requireUser(),{id}=await params;const key={userId_listingId:{userId:u.id,listingId:id}};const x=await db.favorite.findUnique({where:key});if(x)await db.favorite.delete({where:key});else await db.favorite.create({data:{userId:u.id,listingId:id}});return NextResponse.json({favorite:!x});}catch{return NextResponse.json({error:"Login required"},{status:401});}}
