import{redirect}from"next/navigation";import{getUser}from"@/lib/auth";import{db}from"@/lib/db";import Link from"next/link";import{MessageForm}from"@/components/ClientActions";export default async function Messages({searchParams}:{searchParams:Promise<{with?:string}>}){const u=await getUser();if(!u)redirect("/login");const q=await searchParams;const raw=await db.message.findMany({where:{OR:[{senderId:u.id},{recipientId:u.id}]},include:{sender:true,recipient:true},orderBy:{createdAt:"asc"}});const contacts=new Map<string,{id:string,name:string}>();raw.forEach(m=>{const x=m.senderId===u.id?m.recipient:m.sender;contacts.set(x.id,x)});const active=q.with||contacts.keys().next().value;const target=active?await db.user.findUnique({where:{id:active}}):null;const thread=raw.filter(m=>(m.senderId===u.id&&m.recipientId===active)||(m.senderId===active&&m.recipientId===u.id));return <section className="section page"><span className="eyebrow">CONVERSATIONS</span><h1>Messages</h1><div className="inbox"><aside>{[...contacts.values()].map(c=><Link className={c.id===active?"active":""} key={c.id} href={`/messages?with=${c.id}`}>{c.name}</Link>)}{!contacts.size&&<p>Your conversations will appear here.</p>}</aside><article>{target&&<><h2>{target.name}</h2><div className="thread">{thread.map(m=><div className={m.senderId===u.id?"mine":""} key={m.id}><p>{m.body}</p><small>{m.createdAt.toLocaleString()}</small></div>)}</div><MessageForm recipientId={target.id}/></>}</article></div></section>}
