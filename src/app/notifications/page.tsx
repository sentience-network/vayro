import Link from"next/link";import{redirect}from"next/navigation";import{getUser}from"@/lib/auth";import{db}from"@/lib/db";import{ApiButton}from"@/components/ClientActions";export default async function Notifications(){const u=await getUser();if(!u)redirect("/login");const notes=await db.notification.findMany({where:{userId:u.id},orderBy:{createdAt:"desc"},take:100});return <section className="section page narrow"><div className="sectionhead"><div><span className="eyebrow">ACTIVITY</span><h1>Notifications</h1></div><ApiButton url="/api/notifications" method="PATCH" body={{all:true}} label="Mark all read" className="outline"/></div><div className="rows">{notes.map(n=><Link href={n.href||"#"} className={`row ${n.readAt?"muted":"unread"}`} key={n.id}><div><span className="pill">{n.type.replaceAll("_"," ")}</span><h3>{n.title}</h3><p>{n.body}</p><small>{n.createdAt.toLocaleString()}</small></div></Link>)}{!notes.length&&<div className="empty"><h3>You’re all caught up</h3><p>Booking and message updates will appear here.</p></div>}</div></section>}
