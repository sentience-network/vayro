import{redirect}from"next/navigation";import{getUser}from"@/lib/auth";import{db}from"@/lib/db";import{SupportForm}from"@/components/FeatureForms";export default async function Support(){const u=await getUser();if(!u)redirect("/login");const tickets=await db.supportTicket.findMany({where:{userId:u.id},orderBy:{createdAt:"desc"}});return <section className="section page narrow"><span className="eyebrow">VAYRO SUPPORT</span><h1>How can we help?</h1><SupportForm/><h2>Your requests</h2><div className="rows">{tickets.map(t=><div className="row" key={t.id}><div><span className="status">{t.status}</span><h3>{t.subject}</h3><p>{t.message}</p><small>{t.createdAt.toLocaleString()}</small></div></div>)}{!tickets.length&&<p className="muted">No support requests yet.</p>}</div></section>}
