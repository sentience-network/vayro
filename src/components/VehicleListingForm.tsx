"use client";import{useRouter}from"next/navigation";import{useState}from"react";export type VehicleDraft={id:string;slug:string;title:string;description:string;category:string;location:string;pricePerDay:number;securityDeposit:number;feePayer:"RENTER"|"OWNER"|"SPLIT";features:string[];rules:string[];deliveryOptions:string[];details:Record<string,string>;photos:{url:string}[]};
async function compress(files:File[]){if(files.length>6)throw new Error("Upload up to 6 photos");return Promise.all(files.filter(x=>x.size).map(file=>new Promise<string>((resolve,reject)=>{if(!/^image\/(jpeg|png|webp)$/.test(file.type))return reject(new Error("Use JPEG, PNG, or WebP photos"));if(file.size>8e6)return reject(new Error("Each photo must be under 8 MB"));const image=new Image(),url=URL.createObjectURL(file);image.onload=()=>{const scale=Math.min(1,1600/image.width,1200/image.height),canvas=document.createElement("canvas");canvas.width=Math.round(image.width*scale);canvas.height=Math.round(image.height*scale);canvas.getContext("2d")?.drawImage(image,0,0,canvas.width,canvas.height);URL.revokeObjectURL(url);const value=canvas.toDataURL("image/webp",.78);if(value.length>16e5){reject(new Error("Compressed photo is too large"));return}resolve(value)};image.onerror=()=>{URL.revokeObjectURL(url);reject(new Error("Could not read a photo"))};image.src=url})))}
export function VehicleListingForm({listing}:{listing?:VehicleDraft}){const router=useRouter(),[error,setError]=useState(""),[busy,setBusy]=useState(false);const csv=(form:FormData,key:string)=>String(form.get(key)||"").split(",").map(x=>x.trim()).filter(Boolean);return <form className="stack panel" onSubmit={async event=>{event.preventDefault();setBusy(true);setError("");const form=new FormData(event.currentTarget);try{const uploaded=await compress(form.getAll("photoFiles").filter(x=>x instanceof File)as File[]),linked=csv(form,"photos"),photoUrls=uploaded.length||linked.length?[...uploaded,...linked]:(listing?.photos.map(x=>x.url)||[]),details=Object.fromEntries(["year","make","model","vin","licensePlate","mileage","seats","transmission","fuelType"].map(key=>[key,String(form.get(key)||"")]));const body={title:form.get("title"),description:form.get("description"),category:form.get("category"),location:form.get("location"),pricePerDay:form.get("pricePerDay"),securityDeposit:form.get("securityDeposit"),feePayer:form.get("feePayer"),features:csv(form,"features"),rules:csv(form,"rules"),deliveryOptions:csv(form,"deliveryOptions"),photoUrls,details};const response=await fetch(listing?`/api/listings/${listing.id}`:"/api/listings",{method:listing?"PATCH":"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)}),result=await response.json();if(!response.ok)throw new Error(result.error);router.push(`/listings/${listing?.slug||result.slug}`)}catch(e){setError((e as Error).message);setBusy(false)}}}><h2>{listing?"Edit vehicle":"Post a real vehicle"}</h2><p className="muted">VIN and license plate support future verification. They are stored privately and never displayed publicly.</p><div className="formgrid"><label>Listing title<input name="title" defaultValue={listing?.title} required/></label><label>Category<select name="category" defaultValue={listing?.category}>{["Car","Truck","SUV","Luxury","RV","Camper van","Travel trailer","Boat","Jet ski","Motorcycle","ATV","UTV","Other"].map(x=><option key={x}>{x}</option>)}</select></label><label>Location<input name="location" defaultValue={listing?.location} required/></label><label>Price / day<input name="pricePerDay" type="number" min="1" defaultValue={listing?.pricePerDay} required/></label><label>Security deposit<input name="securityDeposit" type="number" min="0" defaultValue={listing?.securityDeposit??0} required/></label><label>Marketplace fee<select name="feePayer" defaultValue={listing?.feePayer||"RENTER"}><option value="RENTER">Renter pays fee</option><option value="OWNER">I absorb fee</option><option value="SPLIT">Split fee 50/50</option></select></label><label>Year<input name="year" inputMode="numeric" defaultValue={listing?.details.year}/></label><label>Make<input name="make" defaultValue={listing?.details.make}/></label><label>Model<input name="model" defaultValue={listing?.details.model}/></label><label>VIN<input name="vin" minLength={17} maxLength={17} defaultValue={listing?.details.vin}/></label><label>License plate<input name="licensePlate" maxLength={16} defaultValue={listing?.details.licensePlate}/></label><label>Mileage / hours<input name="mileage" inputMode="numeric" defaultValue={listing?.details.mileage}/></label><label>Seats<input name="seats" type="number" min="1" max="30" defaultValue={listing?.details.seats}/></label><label>Transmission<select name="transmission" defaultValue={listing?.details.transmission}><option value="">Not applicable</option><option>Automatic</option><option>Manual</option></select></label><label>Fuel / power<input name="fuelType" placeholder="Gas, diesel, electric…" defaultValue={listing?.details.fuelType}/></label></div><label>Description<textarea name="description" defaultValue={listing?.description} minLength={30} required/></label><label className="uploadbox">Upload actual vehicle photos <small>Up to 6 JPEG, PNG, or WebP files.</small><input name="photoFiles" type="file" accept="image/jpeg,image/png,image/webp" multiple/></label><label>Or image URLs<textarea name="photos" defaultValue={listing?.photos.filter(x=>!x.url.startsWith("data:")).map(x=>x.url).join(", ")}/></label><p className="notice">No photos? Vayro adds a representative category image marked “not the actual vehicle.”</p><label>Features<input name="features" defaultValue={listing?.features.join(", ")}/></label><label>Rules<input name="rules" defaultValue={listing?.rules.join(", ")}/></label><label>Delivery options<input name="deliveryOptions" defaultValue={listing?.deliveryOptions.join(", ")}/></label>{error&&<p className="error">{error}</p>}<button className="button" disabled={busy}>{busy?"Saving vehicle…":listing?"Save changes":"Publish vehicle"}</button></form>}
