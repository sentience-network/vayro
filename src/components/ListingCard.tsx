import Link from "next/link";import { ApiButton } from "./ClientActions";import{isRepresentative}from"@/lib/listing-images";
type CardListing={id:string;slug:string;title:string;category:string;location:string;pricePerDay:number;details:unknown;photos:{url:string;alt:string|null}[];reviews:{rating:number}[]};
export function ListingCard({l,canFav=false,isFavorite=false,days=0}:{l:CardListing;canFav?:boolean;isFavorite?:boolean;days?:number}){const rating=l.reviews.length?(l.reviews.reduce((sum,x)=>sum+x.rating,0)/l.reviews.length).toFixed(1):"New",representative=isRepresentative(l.details);return <article className="card"><Link href={`/listings/${l.slug}`}><img className="cardphoto" src={l.photos[0]?.url||"/placeholder.svg"} alt={l.photos[0]?.alt||l.title} loading="lazy" decoding="async"/>{representative&&<span className="representative">Representative image · not actual vehicle</span>}<div className="cardbody"><div className="spread"><span className="pill">{l.category}</span><span aria-label={`${rating} star rating`}>★ {rating}</span></div><h3>{l.title}</h3><p>{l.location}</p><strong>${l.pricePerDay} <small>/ day</small></strong>{days>0&&<small className="triptotal">${(l.pricePerDay*days).toLocaleString()} for {days} {days===1?"day":"days"}, before fees</small>}</div></Link>{canFav&&<ApiButton url={`/api/favorites/${l.id}`} label={isFavorite?"♥ Saved":"♡ Save"} className={`save ${isFavorite?"saved":""}`}/>}</article>}
