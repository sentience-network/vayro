"use client";import{useState}from"react";export function FutureProgramButton({program}:{program:"INSURANCE"|"TRACKER"}){const[joined,setJoined]=useState(false),[busy,setBusy]=useState(false),[error,setError]=useState("");return <div><button type="button" className="outline" disabled={joined||busy} aria-busy={busy} onClick={async()=>{if(!window.confirm(program==="TRACKER"?"I understand future tracking must be disclosed to and consented to by renters, and AirTag must never be used to track a person.":"I understand this is an interest list and no insurance coverage is currently provided."))return;setBusy(true);setError("");try{const response=await fetch("/api/future-programs",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({program,consentAcknowledged:true})}),result=await response.json();if(!response.ok)throw new Error(result.error||"Unable to register interest");setJoined(true)}catch(e){setError((e as Error).message)}finally{setBusy(false)}}}>{joined?"Interest registered ✓":busy?"Saving interest…":"Join future interest list"}</button>{error&&<small className="error" role="alert">{error}</small>}</div>}
