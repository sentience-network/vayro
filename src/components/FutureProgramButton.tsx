"use client";import{useState}from"react";export function FutureProgramButton({program}:{program:"INSURANCE"|"TRACKER"}){const[joined,setJoined]=useState(false);return <button className="outline" disabled={joined} onClick={async()=>{if(!window.confirm(program==="TRACKER"?"I understand future tracking must be disclosed to and consented to by renters, and AirTag must never be used to track a person.":"I understand this is an interest list and no insurance coverage is currently provided."))return;const response=await fetch("/api/future-programs",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({program,consentAcknowledged:true})});if(response.ok)setJoined(true);else alert((await response.json()).error)}}>{joined?"Interest registered ✓":"Join future interest list"}</button>}
