import Link from"next/link";export type PolicySection={title:string;body:string;items?:string[]};export function PolicyPage({title,updated="August 12, 2026",sections,children}:{title:string;updated?:string;sections:PolicySection[];children?:React.ReactNode}){return <section className="section page policy"><div className="notice"><b>Review draft:</b> This operational policy is provided for marketplace transparency and must be reviewed by qualified legal counsel before Vayro scales or enters new jurisdictions.</div><span className="eyebrow">VAYRO POLICY</span><h1>{title}</h1><p>Last updated: {updated} · Version 2026-08-12</p>{sections.map(s=><article key={s.title}><h2>{s.title}</h2><p>{s.body}</p>{s.items&&<ul>{s.items.map(i=><li key={i}>{i}</li>)}</ul>}</article>)}{children}<p className="notice">Questions about these terms should be submitted through <Link href="/support">Vayro Support</Link>.</p></section>}
